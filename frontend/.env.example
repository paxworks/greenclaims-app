# Shopify Partner app client ID — placeholder, no app registered yet
# (build spec §12/§13: brand runs under Paxworks, app name "Green Claims by Paxworks")
NEXT_PUBLIC_SHOPIFY_API_KEY=placeholder-api-key

# greenclaims-api's public URL — install link points here
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
